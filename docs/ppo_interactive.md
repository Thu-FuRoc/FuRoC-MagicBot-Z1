## PPO 学习
[你已知道什么](#ch0) [策略梯度直觉](#ch1) [PPO Clip 核心](#ch2) [GAE 优势估计](#ch3) [完整训练流程](#ch4) [Z1 参数对照](#ch5) 

# PPO 互动学习手册

Proximal Policy Optimization — 结合 MagicBot Z1 训练参数，从直觉到公式，逐步理解。

## 0 你已经知道什么

在 Z1 的训练里，你已经每天都在用 PPO 了——只是没有拆开看过它的内部结构。

### 你的训练命令
 python train.py --task Magiclab-Z1-12dof-Velocity --num_envs 16384 --headless 

运行这条命令后，背后发生的事情是：
16,384 个 Z1 机器人同时在仿真中走路每个 20s / 1000 步 / 50Hz 控制 ↓ 收集每一步的观测、动作、奖励235-dim 观测 × 12-dim 动作 × 标量奖励 ↓ PPO 算法更新策略网络Actor(235→512→256→128→12) 参数微调 ↓ 新策略控制机器人，开始下一轮期望走得更好、更稳、更省力 

这份教程的目标就是让你彻底理解**第 3 步**——PPO 算法到底在做什么。

## 1 策略梯度直觉

### 策略 π 是什么？

策略是一个函数：**给定观测 → 输出动作的概率分布**。对 Z1 来说，输入 235 维观测，输出 12 个关节各自的高斯分布（均值 + 标准差）。

为了直观理解，下面把动作简化为 **1 维**（比如"左膝弯曲程度"），策略就是一个高斯分布曲线。点击按钮采样，看分布如何更新。
采样一个动作 重置 学习率 0.08 绿色区域 = 高奖励。采样后，策略（蓝色曲线）会朝奖励更高的方向移动。这就是策略梯度的核心思想。 

### 直觉总结
**策略梯度一句话：**采样动作 → 看奖励 → 增大好动作的概率，减小坏动作的概率。 

问题是：如果学习率太大，一步更新太猛，策略可能崩溃。这就是 PPO 要解决的核心问题。

## 2 PPO Clip — 核心创新

### 问题：更新太大会崩溃

如果策略一步变太多（比如突然把所有概率都翻转了），机器人行为会突变，之前学到的全部白费。这叫**策略崩溃**。

### 解法：限制变化幅度

PPO 引入了一个**概率比**：
r(θ) = π新(a|s) / π旧(a|s) 

`r = 1.0` 表示新旧策略一样。`r = 2.0` 表示新策略认为这个动作的概率是旧的 2 倍。

PPO 的目标函数：
 LCLIP = E[ min( r·A , clip(r, 1−ε, 1+ε)·A ) ] 

其中 `A` 是优势函数（这个动作比平均水平好多少），`ε` 是 clip 范围（Z1 用 0.2）。

### 互动：拖动滑块理解 Clip

下面的图展示了 **对于单个 (r, A) 组合**，目标函数的形状。调整参数观察：
r·A 无裁剪 clip(r)·A 裁剪后 PPO 实际目标 min(·,·) 裁剪激活区 ε (clip 范围) 0.20 A (优势) 1.5 A > 0（好动作）：clip 防止 r 变得太大，即防止"好动作概率暴增"。 | 灰色虚线标记 r=1（新旧策略相同）。 **关键理解：**当 r 在 [1-ε, 1+ε] 范围内时，PPO 和普通策略梯度一样。一旦超出，目标函数被"削平"，梯度为零，**策略不会再往那个方向更新**。这就是"近端 (Proximal)"的含义——新策略不能离旧策略太远。 

## 3 GAE — 优势估计

### 什么是"优势"？

优势 `A(s,a)` 回答这个问题：**"这个动作比平均水平好多少？"**

- A > 0 → 比平均好，PPO 会**增大**它的概率 
- A < 0 → 比平均差，PPO 会**减小**它的概率 
- A = 0 → 平平无奇，不更新 

### 怎么算优势？两难选择

最直觉的两种极端：

• **TD(0)**：只用"当前奖励 + 下一状态的价值估计"。方差低，但偏差大（依赖价值函数准不准）。

• **Monte Carlo**：等整个 episode 跑完，累加所有未来奖励。偏差低，但方差大（一条轨迹可能很 noisy）。

**GAE (Generalized Advantage Estimation)** 用参数 `λ` 在两者之间插值：
 Ât = δt + (γλ)δt+1 + (γλ)2δt+2 + ... 其中 δt = rt + γV(st+1) − V(st) 是 TD 残差 

### 互动：调节 λ 看效果

下面模拟了一条 10 步轨迹的奖励。拖动 λ 滑块，观察每步的优势估计如何变化。
λ (GAE) 0.95 γ (折扣) 0.99 上：轨迹奖励 (蓝色柱) 和价值函数估计 (灰色虚线)。下：各步的优势估计。λ 越大，优势"传播"越远（低偏差高方差）；λ 越小，只看近处（高偏差低方差）。 **Z1 的配置：**γ=0.99, λ=0.95。这是 RL locomotion 的经典组合——接近 MC 但略有平滑，适合连续控制任务。 

## 4 完整 PPO 训练循环

把前三节串起来，就是一次完整的 PPO 更新。点击"下一步"逐步看。
1. 环境交互 — 采集数据16384 个环境各跑 24 步 → 共 393,216 条 (s, a, r, s') 转移 ↓ 2. 计算回报 & 优势GAE(λ=0.95) 计算每步优势 Ât，折扣回报 Rt ↓ 3. 计算旧策略概率记录 log πold(a|s)，后续 ratio r = πnew/πold ↓ 4. PPO 更新 (5 epochs × 4 mini-batches)每个 mini-batch 24,576 条数据，计算 clip loss + value loss + entropy ↓ 5. 梯度下降，更新 Actor + Criticlr=1e-3, max_grad_norm=1.0 → 策略微微变好 ↓ 6. 回到第 1 步，用新策略采集数据重复 50,000 次迭代 下一步 → 重置 ▶ 自动播放 点击"下一步"开始。 

### 一次迭代的数据量
参数值含义 num_envs16384同时并行的仿真环境数 num_steps_per_env24每次采集每个环境跑 24 步 batch_size393,21616384 × 24 = 总转移数 num_mini_batches4分成 4 个 mini-batch mini_batch_size98,304每次梯度更新的数据量 num_learning_epochs5同一批数据用 5 遍（off-policy reuse） clip_param (ε)0.2概率比裁剪范围 [0.8, 1.2] 

## 5 Z1 参数 ↔ PPO 概念对照

### 网络结构
组件结构说明 Actor (策略网络)235&to;512&to;256&to;128&to;12输入 235-dim 观测 → 输出 12 个关节的均值 Critic (价值网络)237&to;512&to;256&to;128&to;1比 Actor 多 2 维输入 (contact_mask) 激活函数ELU比 ReLU 平滑，梯度更友好 初始噪声 std1.0动作 = 均值 + N(0,1) × std，std 可学习 

### 训练超参数
参数值对应 PPO 概念 learning_rate1e-3Adam 优化器步长，adaptive schedule 自动调节 gamma (γ)0.99折扣因子：未来奖励的重要程度 lam (λ)0.95GAE 参数：偏差-方差权衡（第 3 章） clip_param (ε)0.2PPO clip 范围（第 2 章） entropy_coef0.01鼓励探索：熵越大动作越随机 value_loss_coef1.0Critic loss 权重 desired_kl0.01自适应学习率的目标 KL 散度 max_grad_norm1.0梯度裁剪，防止梯度爆炸 empirical_normalizationFalse不使用经验归一化（Z1 自己在观测里做了缩放） 

### 奖励 → 优势 → 策略 的信息流
20+ 奖励项加权求和track_vel + alive + energy + orientation + ... ↓ GAE 计算优势 Âtδt + (γλ)δt+1 + ... ↓ PPO Clip 目标min(r·A, clip(r)·A) × entropy bonus ↓ 梯度下降更新 Actor 参数235&to;512&to;256&to;128&to;12 权重微调 **调参直觉：** • 训练不稳定/崩了 → 减小 lr、减小 clip_param • 学太慢 → 增大 lr、增大 num_steps_per_env • 动作太随机不收敛 → 减小 entropy_coef • 陷入局部最优、动作太僵 → 增大 entropy_coef