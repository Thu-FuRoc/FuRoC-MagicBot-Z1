# Phase Orchestrator — 后台视频录制 Pipeline

## _run_post_phase_pipeline() · 每个子阶段训练完成后自动触发
训练完成 _handle_overfitting() → status=complete 1 JIT Policy 导出 _export_jit(sp_id, best_ckpt) export_jit.py → policy.pt (~1.1MB) timeout: 120s jit_path 2 MuJoCo Sim2Sim 录制 _record_mujoco_video(sp_id, jit_path) mujoco_manual.py --policy --record EGL offscreen · 500 steps · ~2 min timeout: 300s → {sp_id}_mujoco.mp4 3 Isaac Sim 录制 _record_video(sp_id, best_ckpt) play_z1_video.py --checkpoint --headless 200 frames · camera tracking · ~15-20 min ⚠ 需要先等 Kit 进程退出 (_wait_for_kit_cleanup) 4 视频打标签 _label_videos(sp_id, metrics) label_video.py → 半透明 overlay 标签内容: run, model, time_out, bad_ori, vel_err, ep_len, reward, action_rate Isaac + MuJoCo 两个视频都打标签 5 生成学习曲线 _generate_plots(sp_id, run_dir) plot_learning_curves.py → plots/{sp_id}/ · timeout: 120s 进入下一子阶段 _advance() → status=pending → _start_sub_phase() _parse_training_metrics() 从训练日志最后 8KB 解析指标: Episode_Termination/time_out → time_out Episode_Termination/bad_orientation → bad_ori Metrics/base_velocity/error_vel_xy → vel_err Mean episode length → ep_len Mean reward → reward Episode_Reward/action_rate_l1 → action_rate _wait_for_kit_cleanup() 等待 Omniverse Kit 进程完全退出 避免 KVDB 锁冲突 · timeout: 60s 超时则 SIGKILL 强制清理 输出路径 (RTX 服务器) videos/phase_pipeline/{sp_id}.mp4 videos/phase_pipeline/{sp_id}_mujoco.mp4 → 打标签后原地覆盖 → Claude 自动 SCP 下载到本地 Orchestrator 主循环状态机 pending running overfitting complete failed start detect handle advance _run_post_phase_pipeline() JIT → MuJoCo → Isaac → Label → Plots → next pending 自动回滚机制 条件: best_reward < starting_reward × 0.95 → 丢弃当前结果，从上一 checkpoint 重试 → learning_rate × 0.5 (降低学习率) → 最多回滚 1 次 回滚失败 → rollback_exhausted → 标记 failed 5-Phase 课程顺序 p1_coarse → p1_fine (Flat — Bootstrap) p2_coarse → p2_fine (Flat — Velocity) p3_coarse → p3_fine (Gentle Terrain) p4_coarse → p4_fine (Rough Terrain) p5_coarse → p5_fine (Full Terrain + Polish) Claude /gpu-train --tail 自动检测并 SCP 下载  JIT Export  MuJoCo EGL  Isaac Sim  Label  Plots  状态转换